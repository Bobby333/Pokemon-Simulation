@model IEnumerable<PokemonSimulation.Models.Moves>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Move_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Move_Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Move_Power)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Move_Type)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Move_Amount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Accuracy)
            </th>
            <th>
                Attack Type
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Move_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Move_Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Move_Power)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Move_Type)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Move_Amount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Accuracy)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Move_dType)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MoveId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MoveId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MoveId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
